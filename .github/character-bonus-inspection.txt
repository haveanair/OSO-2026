CHARACTER BONUS COMPACT INSPECTION
INDEX_LEN=1511638

## PRICE_CONTEXT
HITS=3
1=":"남강 수호패","atk":3,"def":4,"price":420},{"name":"1884 용사의 휘장","atk":5,"def":6,"price":720}]; const LEGEND_WEAPON={name:'전설의 조선무',atk:40}; const LEGEND_ARMOR={name:'황금장바구니방패',def:40}; const RPG_INN_PRICE=30; const RPG_MEDICINE=[{id:'포션',price:80,desc:'HP 완전회복'},{id:'에테르',price:45,desc:'MP 완전회복'},{id:'성수',price:60,desc:'적에게 빛 피해'}]; function legendaryGearReady(){return (run.weaponLv||0)>=RPG_WEAPONS.length-1&&(run.armorLv||0)>=RPG_ARMORS.length-1} function equippedWeapon(){return
2=420},{"name":"1884 용사의 휘장","atk":5,"def":6,"price":720}]; const LEGEND_WEAPON={name:'전설의 조선무',atk:40}; const LEGEND_ARMOR={name:'황금장바구니방패',def:40}; const RPG_INN_PRICE=30; const RPG_MEDICINE=[{id:'포션',price:80,desc:'HP 완전회복'},{id:'에테르',price:45,desc:'MP 완전회복'},{id:'성수',price:60,desc:'적에게 빛 피해'}]; function legendaryGearReady(){return (run.weaponLv||0)>=RPG_WEAPONS.length-1&&(run.armorLv||0)>=RPG_ARMORS.length-1} function equippedWeapon(){return run.radish?LEGEND_WEAPON:RPG_WEAPO
3=,"def":6,"price":720}]; const LEGEND_WEAPON={name:'전설의 조선무',atk:40}; const LEGEND_ARMOR={name:'황금장바구니방패',def:40}; const RPG_INN_PRICE=30; const RPG_MEDICINE=[{id:'포션',price:80,desc:'HP 완전회복'},{id:'에테르',price:45,desc:'MP 완전회복'},{id:'성수',price:60,desc:'적에게 빛 피해'}]; function legendaryGearReady(){return (run.weaponLv||0)>=RPG_WEAPONS.length-1&&(run.armorLv||0)>=RPG_ARMORS.length-1} function equippedWeapon(){return run.radish?LEGEND_WEAPON:RPG_WEAPONS[clamp(run.weaponLv||0,0,RPG_WEA

## NONGAE_CONTEXT
HITS=4
1=","note":"깃발을 높이 든 하모","group":"hamo","badge":"하모"},{"id":"nongae_jade","name":"옥가락지 논개","price":3200,"src":"assets/nongae-01.webp?v=20260904-nongae3","group":"nongae","badge":"논개","note":""},{"id":"nongae_resolve","name":"결의의 논개","price":3800,"src":"assets/nongae-02.webp?v=20260904-nongae3","group":"nongae","badge":"논개","note":""}]; const GAME_AC
2=tp7Kh+c6U3LJDwvUogRIDXow/3RLo1WFdqt9Wo/83AjLYHdvDnF3I//r5krh9b/AgdOaKTbFGQM6J/itzncmn9CtRju8oLoB+e9c322llt+uAsFbz9HvKckzHWgAJhRnLNkEinInKMnil7JBPw+RPhLFaImeEAAA=","note":"깃발을 높이 든 하모","group":"hamo","badge":"하모"},{"id":"nongae_jade","name":"옥가락지 논개","price":3200,"src":"assets/nongae-01.webp?v=20260904-nongae3","group":"nongae","badge":"논개","note":""},{"id":"nongae_resolve","name":"결의의 논개","price":3800,"src":"assets/nongae-02.webp?v=20260904-nongae3","group":"nongae","badge":"논개","note":""}]; const GAME_ACQUIRE=[ {code:'basket',place:'진주중앙시장 주차장',detail:'주차장 구역에 들어오면 획득',confirmed:true}, {code:'fish',place:'3구 어시장 인근 전부',detail:'3구 어시장 인근 통로 범
3=),selected=state.selectedSkin===s.id; return `<div class="skinCard ${selected?'selected':''} group-${gr.id}" data-skin="${s.id}"><span class="charBadge ${gr.id}">${s.badge||gr.title}</span><img src="${s.src}" alt="${s.name}" style="${lockedGroup?'visibility:hidden;filter:grayscale(1);opacity:0':''}"><b>${s.name}</b>${s.note?'<small>'+s.note+'</small>':(['nongae_jade','nongae_resolve'].includes(s.id)?'<small aria-hidden="true">&nbsp;</small>':'')}<button class="${selected?'selectedBtn':owned?'owned':''}" data-buy="${s.id}" ${lockedGroup?'disabled':''}>${lockedGroup?'🔒':selected?'사용 중':owned?'사용하기':`🪙 ${s.price.toLocaleString()} 구입`}</button></
4=te.selectedSkin===s.id; return `<div class="skinCard ${selected?'selected':''} group-${gr.id}" data-skin="${s.id}"><span class="charBadge ${gr.id}">${s.badge||gr.title}</span><img src="${s.src}" alt="${s.name}" style="${lockedGroup?'visibility:hidden;filter:grayscale(1);opacity:0':''}"><b>${s.name}</b>${s.note?'<small>'+s.note+'</small>':(['nongae_jade','nongae_resolve'].includes(s.id)?'<small aria-hidden="true">&nbsp;</small>':'')}<button class="${selected?'selectedBtn':owned?'owned':''}" data-buy="${s.id}" ${lockedGroup?'disabled':''}>${lockedGroup?'🔒':selected?'사용 중':owned?'사용하기':`🪙 ${s.price.toLocaleString()} 구입`}</button></div>` }).join(

## SKIN_WORD_CONTEXT
HITS=0

## SELECTED_CONTEXT
HITS=15
1=lng))*Math.cos(rad(b.lat)); const x=Math.cos(rad(a.lat))*Math.sin(rad(b.lat))-Math.sin(rad(a.lat))*Math.cos(rad(b.lat))*Math.cos(rad(b.lng-a.lng)); return (Math.atan2(y,x)*180/Math.PI+360)%360 } function arrowForBearing(d){ const arrows=['↑','↗','→','↘','↓','↙','←','↖'];return arrows[Math.round(d/45)%8] } function currentSkin(){return OSO_SKINS.find(x=>x.id===state.selectedSkin)||OSO_SKINS[0]} function currentOsoSrc(){return currentSkin().src} const BASE_GAME_CODES=['basket','fish','stack','run','slice','tteok','powerduck']; const BONUS_GAME_CODES=['spacefighter','racing','fantasy']; function ensureSpacefighterUnlock(){ if(state.unlock&&s
2=rad(a.lat))*Math.sin(rad(b.lat))-Math.sin(rad(a.lat))*Math.cos(rad(b.lat))*Math.cos(rad(b.lng-a.lng)); return (Math.atan2(y,x)*180/Math.PI+360)%360 } function arrowForBearing(d){ const arrows=['↑','↗','→','↘','↓','↙','←','↖'];return arrows[Math.round(d/45)%8] } function currentSkin(){return OSO_SKINS.find(x=>x.id===state.selectedSkin)||OSO_SKINS[0]} function currentOsoSrc(){return currentSkin().src} const BASE_GAME_CODES=['basket','fish','stack','run','slice','tteok','powerduck']; const BONUS_GAME_CODES=['spacefighter','racing','fantasy']; function ensureSpacefighterUnlock(){ if(state.unlock&&state.unlock.spacefighter)return false; if(BASE_GA
3=b.lat))*Math.cos(rad(b.lng-a.lng)); return (Math.atan2(y,x)*180/Math.PI+360)%360 } function arrowForBearing(d){ const arrows=['↑','↗','→','↘','↓','↙','←','↖'];return arrows[Math.round(d/45)%8] } function currentSkin(){return OSO_SKINS.find(x=>x.id===state.selectedSkin)||OSO_SKINS[0]} function currentOsoSrc(){return currentSkin().src} const BASE_GAME_CODES=['basket','fish','stack','run','slice','tteok','powerduck']; const BONUS_GAME_CODES=['spacefighter','racing','fantasy']; function ensureSpacefighterUnlock(){ if(state.unlock&&state.unlock.spacefighter)return false; if(BASE_GAME_CODES.every(c=>state.unlock&&state.unlock[c])){ state.unlo
4=localStorage.setItem(DEV_ALL_UNLOCKED?'jcm-v168-unlocked':'jcm-v168-locked',JSON.stringify(state))}catch(e){}} let state=load()||{unlock:{},best:{},coins:0,todayEarned:0,todayScore:0,scoreDate:localDateKey(),pdRecord:0,coinDate:localDateKey(),ranking:[],todaySubmittedBest:0,rankRegisteredBest:11,ownedSkins:['default'],selectedSkin:'default'}; if(!state.best||typeof state.best!=='object')state.best={}; if(!Array.isArray(state.ranking))state.ranking=[]; if(!Array.isArray(state.ownedSkins))state.ownedSkins=['default']; if(typeof state.nongaeUnlocked!=='boolean')state.nongaeUnlocked=false; if(!state.nongaeUnlocked){try{const d=JSON.parse(localSto
5=y.isArray(d.run.bossDefeated)&&d.run.bossDefeated[6]))){state.nongaeUnlocked=true;save()}}catch(_){}} if(!state.gpsCalibration)state.gpsCalibration={}; if(!state.gpsUnlocked)state.gpsUnlocked={}; if(!state.qrUnlocked)state.qrUnlocked={}; if(!state.ownedSkins.includes('default'))state.ownedSkins.unshift('default'); if(!state.selectedSkin||!state.ownedSkins.includes(state.selectedSkin))state.selectedSkin='default'; if(state.todayEarned==null)state.todayEarned=0; if(state.todayScore==null)state.todayScore=0; if(!state.scoreDate)state.scoreDate=localDateKey(); if(state.todaySubmittedBest==null)state.todaySubmittedBest=0; if(state.rankRegisteredBe
6=ed[6]))){state.nongaeUnlocked=true;save()}}catch(_){}} if(!state.gpsCalibration)state.gpsCalibration={}; if(!state.gpsUnlocked)state.gpsUnlocked={}; if(!state.qrUnlocked)state.qrUnlocked={}; if(!state.ownedSkins.includes('default'))state.ownedSkins.unshift('default'); if(!state.selectedSkin||!state.ownedSkins.includes(state.selectedSkin))state.selectedSkin='default'; if(state.todayEarned==null)state.todayEarned=0; if(state.todayScore==null)state.todayScore=0; if(!state.scoreDate)state.scoreDate=localDateKey(); if(state.todaySubmittedBest==null)state.todaySubmittedBest=0; if(state.rankRegisteredBest==null){ state.rankRegisteredBest=Number.isFi
7=eUnlocked=true;save()}}catch(_){}} if(!state.gpsCalibration)state.gpsCalibration={}; if(!state.gpsUnlocked)state.gpsUnlocked={}; if(!state.qrUnlocked)state.qrUnlocked={}; if(!state.ownedSkins.includes('default'))state.ownedSkins.unshift('default'); if(!state.selectedSkin||!state.ownedSkins.includes(state.selectedSkin))state.selectedSkin='default'; if(state.todayEarned==null)state.todayEarned=0; if(state.todayScore==null)state.todayScore=0; if(!state.scoreDate)state.scoreDate=localDateKey(); if(state.todaySubmittedBest==null)state.todaySubmittedBest=0; if(state.rankRegisteredBest==null){ state.rankRegisteredBest=Number.isFinite(state.dailyRank
8=.gpsCalibration||{},null,2) } $('#gpsStartBtn').onclick=startGpsWatch; $('#calStart').onclick=()=>saveCalPoint('a');$('#calEnd').onclick=()=>saveCalPoint('b');$('#calCenter').onclick=()=>saveCalPoint('center'); renderGpsLocations() } function renderOsoShop(){ const shop=$('#osoShop');if(!shop)return; const skin=currentSkin(),preview=$('#selectedOsoPreview'),name=$('#selectedOsoName'),coin=$('#shopCoin'); if(preview)preview.src=skin.src;if(name)name.textContent=skin.name;if(coin)coin.textContent=state.coins||0; const groups=[ {id:'oso',title:'🐂 오소',sub:'중앙시장의 주인공 · 기본~750 코인'}, {id:'ayo',title:'🐱 아요 컬렉션',sub:'표정과 동작이 다양한 친구 · 1,000
9=구 · 2,000~2,900 코인'}, {id:'nongae',title:'🌊 논개 컬렉션',sub:'해금조건 : 오소 환상대모험 이벤트 클리어 · 3,000 코인대'} ]; shop.innerHTML=groups.map(gr=>{ const lockedGroup=gr.id==='nongae'&&!state.nongaeUnlocked; const cards=OSO_SKINS.filter(s=>(s.group||'oso')===gr.id).map(s=>{ const owned=state.ownedSkins.includes(s.id),selected=state.selectedSkin===s.id; return `<div class="skinCard ${selected?'selected':''} group-${gr.id}" data-skin="${s.id}"><span class="charBadge ${gr.id}">${s.badge||gr.title}</span><img src="${s.src}" alt="${s.name}" style="${lockedGroup?'visibility:hidden;filter:grayscale(1);opacity:0':''}"><b>${s.name}</b>${s.note?'<small>'+s.note+'</
10=${gr.id}"><b>${gr.title}</b><small>${gr.sub}</small></div>${cards}` }).join(''); $$('[data-buy]').forEach(btn=>btn.onclick=()=>{ const s=OSO_SKINS.find(x=>x.id===btn.dataset.buy);if(!s)return;if((s.group||'oso')==='nongae'&&!state.nongaeUnlocked){tone('bad');buzz(40);return} if(state.ownedSkins.includes(s.id)){state.selectedSkin=s.id;save();refresh();tone('good');return} if((state.coins||0)<s.price){tone('bad');buzz(70);btn.textContent='코인이 부족하소!';setTimeout(renderOsoShop,700);return} state.coins-=s.price;state.ownedSkins.push(s.id);state.selectedSkin=s.id;save();tone('clear');buzz([20,15,35]);refresh();try{if(window.__JCM_SP1_SYNC_UNLO
11=te.nongaeUnlocked){tone('bad');buzz(40);return} if(state.ownedSkins.includes(s.id)){state.selectedSkin=s.id;save();refresh();tone('good');return} if((state.coins||0)<s.price){tone('bad');buzz(70);btn.textContent='코인이 부족하소!';setTimeout(renderOsoShop,700);return} state.coins-=s.price;state.ownedSkins.push(s.id);state.selectedSkin=s.id;save();tone('clear');buzz([20,15,35]);refresh();try{if(window.__JCM_SP1_SYNC_UNLOCK__)window.__JCM_SP1_SYNC_UNLOCK__()}catch(_){} }) } function refresh(){ ensureDailyScoreDate(); if(state.coinDate!==localDateKey()){ state.coinDate=localDateKey();save() } if(DEV_ALL_UNLOCKED)GAMES.forEach(g=>state.unlock[g
12=e;coins++;score+=30;const p=coinPatterns.get(q.pattern);if(p&&!p.done){p.collected++;completePattern(q.pattern)}tone('coin');buzz(10)}else if(!q.g&&q.x<=-30){missPattern(q.pattern)}});coinList=coinList.filter(q=>!q.g&&q.x>-30); let y=ch-157+py,sY=land>0?.82:1,sX=land>0?1.12:1; if(img.complete){ const runnerSkin=currentSkin(); const flipRunner=runnerSkin.id!=='ayo_glasses'; ctx.save();ctx.translate(84,y+45);ctx.scale(flipRunner?-sX:sX,sY); if(inv>0&&Math.floor(ts/90)%2===0)ctx.globalAlpha=.38;ctx.drawImage(img,-47,-47,94,94);ctx.restore() } $('#gScore').textContent=Math.floor(score+coins*30);$('#runDist').textContent=`${Math
13=imationFrame(raf)} } /* ===== 오소 우주파이터 : 5-STAGE VERTICAL SHOOTER ===== */ function beginSpacefighter(){ const unlocked=!!state.spacefighterKf21Unlocked; if(!unlocked){state.spacefighterCraft='character';save();$('#stage').innerHTML='';readyThen(()=>playSpacefighter('character'));return} const s=$('#stage'),skin=currentSkin(),isNew=!!state.spacefighterKf21New; s.innerHTML=`<div class="sfCraftSelect"> <div class="sfCraftTitle">출격 선택</div> <div class="sfCraftSub">캐릭터 또는 KF-21 전투기를 선택하시오</div> <div class="sfCraftGrid"> <button class="sfCraftCard character" id="sfChooseCharacter"><div class="sfCraftImgBox"><img class="sfCraftImg" s
14=cefighterKf21New=false;save();s.innerHTML='';readyThen(()=>playSpacefighter(mode))}; $('#sfChooseCharacter').onclick=()=>choose('character'); $('#sfChooseKf21').onclick=()=>choose('kf21'); cleanup=()=>{}; } function playSpacefighter(craftMode='character'){ const isKf21=craftMode==='kf21'; const s=$('#stage'),skin=currentSkin(),group=skin.group||'oso'; const isNongae=group==='nongae'; const shotIcon=group==='oso'?'📦':'●'; const superName=isKf21?'스카이 블레이즈!':(isNongae?'천상의 옥가락지 어택!':(group==='oso'?'만물상 폭격!':'진주 폭풍!')); s.innerHTML=`<div class="sfWrap"> <canvas id="sfCanvas"></canvas> <div class="sfHud"><div id="sfScore">SCORE 0</d
15=window.removeEventListener('pointerup',driftUp,true);window.removeEventListener('pointerup',steerUp,true); boostBtn.removeEventListener('pointerdown',boostDown);boostBtn.onclick=null;stopEngine();stopBGM() } } function playOsoFantasy(){ const s=$('#stage'),heroImg=new Image();heroImg.src=currentOsoSrc(),heroGroup=currentSkin().group||'oso'; s.innerHTML=`<div class="fantasyWrap"><canvas id="fantasyCanvas"></canvas><div class="fantasyHud"><div id="fantasyStat">LV 1 · HP 20/20 · MP 6/6 · EXP 0/8</div><div class="fantasyPlace" id="fantasyPlace">중앙시장</div></div><button class="fantasyMapBtn" id="fantasyMapBtn">MAP</button><button class="fantas

## OWNED_CONTEXT
HITS=9
1=?'jcm-v168-unlocked':'jcm-v168-locked',JSON.stringify(state))}catch(e){}} let state=load()||{unlock:{},best:{},coins:0,todayEarned:0,todayScore:0,scoreDate:localDateKey(),pdRecord:0,coinDate:localDateKey(),ranking:[],todaySubmittedBest:0,rankRegisteredBest:11,ownedSkins:['default'],selectedSkin:'default'}; if(!state.best||typeof state.best!=='object')state.best={}; if(!Array.isArray(state.ranking))state.ranking=[]; if(!Array.isArray(state.ownedSkins))state.ownedSkins=['default']; if(typeof state.nongaeUnlocked!=='boolean')st
2=oinDate:localDateKey(),ranking:[],todaySubmittedBest:0,rankRegisteredBest:11,ownedSkins:['default'],selectedSkin:'default'}; if(!state.best||typeof state.best!=='object')state.best={}; if(!Array.isArray(state.ranking))state.ranking=[]; if(!Array.isArray(state.ownedSkins))state.ownedSkins=['default']; if(typeof state.nongaeUnlocked!=='boolean')state.nongaeUnlocked=false; if(!state.nongaeUnlocked){try{const d=JSON.parse(localStorage.getItem('jcm-fantasy-save-v200')||'null');if(d&&d.run&&(d.run.nongaDefeated||(Array.isArray(d.r
3=ey(),ranking:[],todaySubmittedBest:0,rankRegisteredBest:11,ownedSkins:['default'],selectedSkin:'default'}; if(!state.best||typeof state.best!=='object')state.best={}; if(!Array.isArray(state.ranking))state.ranking=[]; if(!Array.isArray(state.ownedSkins))state.ownedSkins=['default']; if(typeof state.nongaeUnlocked!=='boolean')state.nongaeUnlocked=false; if(!state.nongaeUnlocked){try{const d=JSON.parse(localStorage.getItem('jcm-fantasy-save-v200')||'null');if(d&&d.run&&(d.run.nongaDefeated||(Array.isArray(d.run.bossDefeated)&&
4=eated||(Array.isArray(d.run.bossDefeated)&&d.run.bossDefeated[6]))){state.nongaeUnlocked=true;save()}}catch(_){}} if(!state.gpsCalibration)state.gpsCalibration={}; if(!state.gpsUnlocked)state.gpsUnlocked={}; if(!state.qrUnlocked)state.qrUnlocked={}; if(!state.ownedSkins.includes('default'))state.ownedSkins.unshift('default'); if(!state.selectedSkin||!state.ownedSkins.includes(state.selectedSkin))state.selectedSkin='default'; if(state.todayEarned==null)state.todayEarned=0; if(state.todayScore==null)state.todayScore=0; if(!sta
5=ted)&&d.run.bossDefeated[6]))){state.nongaeUnlocked=true;save()}}catch(_){}} if(!state.gpsCalibration)state.gpsCalibration={}; if(!state.gpsUnlocked)state.gpsUnlocked={}; if(!state.qrUnlocked)state.qrUnlocked={}; if(!state.ownedSkins.includes('default'))state.ownedSkins.unshift('default'); if(!state.selectedSkin||!state.ownedSkins.includes(state.selectedSkin))state.selectedSkin='default'; if(state.todayEarned==null)state.todayEarned=0; if(state.todayScore==null)state.todayScore=0; if(!state.scoreDate)state.scoreDate=localDat
6=)}}catch(_){}} if(!state.gpsCalibration)state.gpsCalibration={}; if(!state.gpsUnlocked)state.gpsUnlocked={}; if(!state.qrUnlocked)state.qrUnlocked={}; if(!state.ownedSkins.includes('default'))state.ownedSkins.unshift('default'); if(!state.selectedSkin||!state.ownedSkins.includes(state.selectedSkin))state.selectedSkin='default'; if(state.todayEarned==null)state.todayEarned=0; if(state.todayScore==null)state.todayScore=0; if(!state.scoreDate)state.scoreDate=localDateKey(); if(state.todaySubmittedBest==null)state.todaySubmitted
7=id:'nongae',title:'🌊 논개 컬렉션',sub:'해금조건 : 오소 환상대모험 이벤트 클리어 · 3,000 코인대'} ]; shop.innerHTML=groups.map(gr=>{ const lockedGroup=gr.id==='nongae'&&!state.nongaeUnlocked; const cards=OSO_SKINS.filter(s=>(s.group||'oso')===gr.id).map(s=>{ const owned=state.ownedSkins.includes(s.id),selected=state.selectedSkin===s.id; return `<div class="skinCard ${selected?'selected':''} group-${gr.id}" data-skin="${s.id}"><span class="charBadge ${gr.id}">${s.badge||gr.title}</span><img src="${s.src}" alt="${s.name}" style="${lockedGro
8=l>${gr.sub}</small></div>${cards}` }).join(''); $$('[data-buy]').forEach(btn=>btn.onclick=()=>{ const s=OSO_SKINS.find(x=>x.id===btn.dataset.buy);if(!s)return;if((s.group||'oso')==='nongae'&&!state.nongaeUnlocked){tone('bad');buzz(40);return} if(state.ownedSkins.includes(s.id)){state.selectedSkin=s.id;save();refresh();tone('good');return} if((state.coins||0)<s.price){tone('bad');buzz(70);btn.textContent='코인이 부족하소!';setTimeout(renderOsoShop,700);return} state.coins-=s.price;state.ownedSkins.push(s.id);state.sele
9=);return} if(state.ownedSkins.includes(s.id)){state.selectedSkin=s.id;save();refresh();tone('good');return} if((state.coins||0)<s.price){tone('bad');buzz(70);btn.textContent='코인이 부족하소!';setTimeout(renderOsoShop,700);return} state.coins-=s.price;state.ownedSkins.push(s.id);state.selectedSkin=s.id;save();tone('clear');buzz([20,15,35]);refresh();try{if(window.__JCM_SP1_SYNC_UNLOCK__)window.__JCM_SP1_SYNC_UNLOCK__()}catch(_){} }) } function refresh(){ ensureDailyScoreDate(); if(state.coinDate!==localDateKey()){ st

## FINISH_DEF
HITS=1
1=:[140,320,560],fish:[140,320,560],stack:[450,820,1200],run:[600,1400,2600],slice:[150,350,620],tteok:[260,560,900],powerduck:[30,50,70],spacefighter:[3500,7500,12000],racing:[7000,13000,22000],fantasy:[8000,16000,26000]}[code]||[100,250,500];return score>=t[2]?3:score>=t[1]?2:score>=t[0]?1:0} function gameClearFanfare(code){ const roots={ basket:523,fish:587,stack:523,run:587,slice:659, tteok:523,powerduck:659,spacefighter:587,racing:659,fantasy:523 }; const r=roots[code]||587; const seq=[ [r,0,.085,'square'], [r*1.25,95,.085,'square'], [r*1.50,190,.095,'triangle'], [r*2.00,305,.13,'square'], [r*1.50,455,.075,'triangle'], [r*2.00,540,.085,'sq

## FANTASY_REWARD
HITS=2
1=class="nongaSpark" style="--x:${x}%;--y:${y}%;--d:${d}s"></i>`} ov.innerHTML=`<div class="nongaBeam"></div><div class="nongaHalo"></div>${sparks}<div class="nongaAscensionAngel">${bossBattleSvg({name:'봉인된 대천사논개'})}</div><div class="nongaAscensionMessage">대천사 논개는 강렬한 빛 속에서 하늘로 승천했다.<b>진주의 영원한 축복을 기원하며 사라졌다.</b></div>`; wrap.appendChild(ov); beep(523,.22,.040,'triangle',784);setTimeout(()=>beep(784,.28,.044,'triangle',1175),380);setTimeout(()=>beep(1047,.38,.047,'triangle',1568),820);setTimeout(()=>buzz([25,12,45,16,75]),980);setTimeout(()=>beep(1319,.62,.042,'sine',1760),1700);setTimeout(()=>beep(1568,.72,.040,'sine',2093),2450); setTimeout(()
2=5,16,75]),980);setTimeout(()=>beep(1319,.62,.042,'sine',1760),1700);setTimeout(()=>beep(1568,.72,.040,'sine',2093),2450); setTimeout(()=>{ov.remove();finishFantasy()},7600) } function fantasyBattleCoinReward(e){ const name=String((e&&e.name)||''); const isBoss=!!(e&&e.boss); const normalByZone=[30,30,45,60,75,90,100]; if(!isBoss)return normalByZone[Math.max(0,Math.min(normalByZone.length-1,zone))]||30; if(name.includes('논개'))return 5000; if(name.includes('암흑장군')||name.includes('암흑 장군'))return 4000; if((e&&e.finalBoss)||name.includes('마왕'))return 3000; const bossByZone=[1000,1000,1500,2000,2500,3000,3000]; return bossByZone[Math.max(0,Math.min

## FINISH_GAME_IDS
IDS=basket,fantasy,fish,powerduck,racing,run,slice,spacefighter,stack,tteok

## CHARACTER_STATE_KEYS
KEYS=ownedSkins,selectedSkin
