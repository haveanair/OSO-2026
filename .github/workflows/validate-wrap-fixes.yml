name: Inspect tteok UI layers

on:
  push:
    branches: [qa-wrap-audit]
    paths:
      - '.github/validate-wrap-fixes.trigger'

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: Inspect current production tteok source
        shell: bash
        run: |
          set -euo pipefail
          curl -fsSL 'https://raw.githubusercontent.com/haveanair/OSO-2026/main/play/index.html' -o /tmp/play.html
          python3 - <<'PY'
          from pathlib import Path
          s=Path('/tmp/play.html').read_text(encoding='utf-8')
          needles=['tteokBtn','meterWrap','meterNeedle','hammer','떡메 내려치기','startTteok','tteokHit','function tteok']
          for needle in needles:
              print('\n===== ',needle,' =====')
              start=0; n=0
              while True:
                  i=s.find(needle,start)
                  if i<0: break
                  n+=1
                  a=max(0,i-1000); b=min(len(s),i+1800)
                  print(f'--- occurrence {n} at {i} ---')
                  print(s[a:b].replace('><','>\n<'))
                  start=i+len(needle)
                  if n>=8: break
              if n==0: print('NOT FOUND')
          PY
