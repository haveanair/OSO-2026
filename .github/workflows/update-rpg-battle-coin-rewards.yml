name: Update RPG battle coin rewards
on:
  push:
    paths:
      - '.github/workflows/update-rpg-battle-coin-rewards.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Credit RPG battle rewards immediately
        run: python3 .github/rpg_reward_wallet_credit.py
      - name: Verify and publish
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          git config user.name 'PWDJ RPG Reward Update'
          git config user.email '34460283+haveanair@users.noreply.github.com'
          git add play/index.html .github/rpg-reward-wallet-credit-report.txt
          git diff --cached --quiet && exit 0
          git commit -m 'Credit RPG battle rewards immediately'
          git push
